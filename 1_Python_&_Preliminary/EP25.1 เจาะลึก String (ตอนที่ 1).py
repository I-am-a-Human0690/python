#เจาะลึกพังชั่น string(str)
#index ระบุตัวอักษรที่จะแสดง ตัวที่หนึ่งเริ่มจาก0 +=เริ่มนับจากข้างหน้า -=เริ่มนับจากข้างหลัง ช่องว่างก้นับ
#ชื่อ[start:stop:step]
name=("sitthikon")
print(name[0])
print(name[+0])
print(name[0:5])#ต้องระบุเลยตัวที่จะแสดงหนึ่งตัว
print(name[:5])#แบบนี้ก้ได้
#นับจากด้านหลัง
print(name[-1])#เริ่มจากหนึ่ง
print(name[-6:])
print(name[:-3])
print(name[::2])
# name[1]="d" ไม่ได้
# print(name)
